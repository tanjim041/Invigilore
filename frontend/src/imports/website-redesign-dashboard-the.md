Redesign the entire website using the existing dashboard theme to create a consistent, modern, and professional UI.

Requirements:

Apply the dashboard color palette, typography, spacing, and components across all pages.

Use the same navigation, header, footer, buttons, form styles, and card components as in the dashboard.

Update the following pages:

Homepage

Login page

Sign Up page

Dashboard

Any additional informational or feature pages

Ensure all UI elements (buttons, links, input fields, tables, cards, icons) match the dashboard style.

Maintain consistent hover effects, shadows, border-radius, and interactive states used in the dashboard.

For forms (login, signup, settings):

Use the same input fields, button styles, and error/validation messages as the dashboard.

Make sure typography hierarchy (headings, subheadings, body text) is consistent across all pages.

Ensure responsive behavior is consistent:

Desktop: full dashboard layout

Tablet: maintain spacing and readability

Mobile: stacked layout with accessible interactions

Keep brand logo, colors, and accent elements consistent on all pages.

Update icons, illustrations, and any graphic elements to match the dashboard style.

Use auto-layout and reusable components to ensure the design system can scale across all pages.

Output:

Full website design with all pages using a unified dashboard theme.

Desktop, tablet, and mobile versions for each page.

Interactive components for hover, focus, and clickable states where relevant.

Maintain usability, readability, and professional SaaS style